[build-system]
requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "IsoFATE"
version = "0.0.1"

dependencies = [
 "numpy",
 "scipy",
 "matplotlib",
 "atmodeller",
 "optimistix",
]